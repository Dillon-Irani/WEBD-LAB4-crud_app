<%= form_with(model: gundam) do |form| %>
  <% if gundam.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gundam.errors.count, "error") %> prohibited this gundam from being saved:</h2>

      <ul>
        <% gundam.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :series, style: "display: block" %>
    <%= form.text_field :series %>
  </div>

  <div>
    <%= form.label :scale, style: "display: block" %>
    <%= form.text_field :scale %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
